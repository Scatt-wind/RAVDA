name: ravda
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - pandas
      - numpy
      - matplotlib
      - python-multipart
      - openpyxl
      - python-dotenv
      - pymysql
